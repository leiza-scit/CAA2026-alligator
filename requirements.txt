# Runtime dependencies for the scripts in py/:
#   · alligator_to_clean_rdf.py      (pandas, matplotlib, networkx, rdflib, shapely)
#   · read_arretine_services.py      (pandas, openpyxl)
#   · events_timeline_by_service.py  (pandas, matplotlib, rdflib, openpyxl)
#   · build_sparql.py                (pyyaml, jinja2, rdflib)
#   · docs_facts.py                  (rdflib)
#   · build_docs.py                  (pyyaml, jinja2, openpyxl, rdflib)
# (all other imports used by the scripts are part of the Python standard library)
#
# Install with:  pip install -r requirements.txt
# Requires Python >= 3.9
#
# Lower bounds are chosen for broad compatibility; the pipeline was last
# verified with the exact versions noted after each entry.

pandas>=2.0        # tested with 3.0.2
matplotlib>=3.7    # tested with 3.10.8
networkx>=3.0      # tested with 3.6.1
rdflib>=7.0        # tested with 7.6.0
shapely>=2.0       # tested with 2.1.2
openpyxl>=3.1      # tested with 3.1.5 — Excel engine used by pandas.read_excel (.xlsx)
pyyaml>=6.0        # tested with 6.0.2 — reads queries.yaml and note.yaml
jinja2>=3.1        # tested with 3.1.4 — renders the query page and the note
